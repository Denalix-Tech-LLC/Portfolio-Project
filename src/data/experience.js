/* =============================================================
   EXPERIENCE
   Managed via the CMS at /admin — content/experience.json.
   ============================================================= */
import data from '../../content/experience.json'

export const experience = data.jobs
