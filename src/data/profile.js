/* =============================================================
   PROFILE & SITE COPY
   Content is now managed via the CMS at /admin and lives in
   content/profile.json — edit it there (or that file) instead of here.
   ============================================================= */
import profile from '../../content/profile.json'

export { profile }
